// Function Expression: Defining a function as a variable

const addition = function () {
  let x = 10;
  let y = 20;
  return x + y;
};

// Function Call: Calling the anonymous function via the variable
console.log(addition());
