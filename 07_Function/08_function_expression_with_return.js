// Function Expression with Return Value: A function that returns the result

const addition = function (x, y) {
  return x + y;
};

// Function Call: Returning and storing the sum in a variable
let sum = addition(50, 60);
console.log(sum); // Logging the result of the function call
