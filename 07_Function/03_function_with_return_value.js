// Function Definition: A function that returns a value

function addition(x, y) {
  let sum = x + y;
  return sum; // Returns the sum instead of logging it
}

// Function Call: Storing the result in a variable
let result = addition(10, 60);

// Logging the returned result
console.log("Sum: ", result);
